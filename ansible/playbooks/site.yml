---

- name: Wait for SSH
  hosts: linode
  gather_facts: false
  tasks:
    - name: Wait for SSH connection
      ansible.builtin.wait_for_connection:
        timeout: 120

- name: Install Stirling PDF
  become: true
  hosts: linode
  roles:
    - docker
